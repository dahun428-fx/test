import { productImageList } from './ProductImageList/ProducgImageList.i18n.en';
import { Translation } from '@/i18n/types';
export const productImagePanel: Translation = {
	mouseOverCaption: 'Move the mouse over the image to zoom',
	clickCaption: 'Click this image to zoom it.',
	productImageList,
};
