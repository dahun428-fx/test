import { Translation } from '@/i18n/types';

export const orderDeadline: Translation = {
	message:
		'Please note that if this product is ordered after {{orderDeadline}}, the order will not be received until the following day.',
};
