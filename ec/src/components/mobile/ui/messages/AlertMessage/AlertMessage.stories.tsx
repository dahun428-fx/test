import { Story } from '@storybook/react';
import { AlertMessage } from './AlertMessage';

export default {
	component: AlertMessage,
};

export const _AlertMessage: Story = () => (
	<AlertMessage>AlertMessage</AlertMessage>
);
