import React from 'react';
import { NotFound } from '@/components/pc/pages/NotFound';

/** Not found page */
const NotFoundPage = () => {
	return <NotFound />;
};

NotFoundPage.displayName = 'NotFoundPage';
export default NotFoundPage;
